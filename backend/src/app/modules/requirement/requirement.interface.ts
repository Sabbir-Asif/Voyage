export type Requirement = {
  boardingPoint: string;
  destination: string;
  company: string;
  ageConcern: string;
  sinceWhen: Date;
  tillWhen: Date;
  budgetType: string;
  preferredActivities: string[];
  userId: string;
};
